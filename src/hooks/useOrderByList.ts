import { useEffect, useState } from "react";
import SortBy from "../interfaces/sortBy";
import OrderBy from "../interfaces/orderBy";

export default function useOrderByList() {
  const [order, setOrder] = useState<any[]>([])
  
}
